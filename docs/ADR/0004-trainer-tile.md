# ADR 0004 — Trainer tile is not an LLM

The trainer is a box: take current best adapters, run a cheaper inner eval, write
a child genome onto a new fly. Surplus calories keep it alive. That is the same
*job* as a hobbyist training a readout. The fly does not contain a language model.
