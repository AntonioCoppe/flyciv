# RUNS

Replay seeds for civilization events. Empty on purpose in v0: the gated path is
`flyciv run --seed 7` (deterministic report) and `--construct-win` (mechanism
check). When someone demonstrates K generations of a live trainer on an unforced
colony, log seed, commit, and `report.json` here.
